# 变更记录

## [已实施] - 2026-07-27

### 新增

- `UI-ITEM-ROW-WATERMARK-003`：项目详情页“分集列表”的每一行分集条目（`fj-review-item-row`）新增两处大号、半透明、艺术斜体背景字，视觉语言沿用 `UI-HISTORY-VERSION-WATERMARK-001` 与 `UI-EPISODE-CARD-COMPACT-002` 已实现的背景字规范：
  - 红框区（紧跟“原文件：… · 当前 Vx”摘要文字之后的行中部空白处）：显示该分集**当前版本**的版本号背景字 `Vx`（如 `V2`），随 `currentVersionId` 动态变化，不取历史版本、不写死。
  - 黄框区（压在“当前未修改 n”前景文字的右端，作为文字的背景）：显示该分集**当前版本未修改意见数量**的大号背景数字 `n`，与前景“当前未修改 n”文案中的数字始终为同一动态计数值。
  - 红框、黄框是用户标注的**精确位置**：背景半透明文字只允许在对应框内排版与裁切，容器 `overflow: hidden`，背景字不得溢出到其他单元格，不得遮挡或改变前景文字、状态胶囊、按钮的层级、可读性与交互（`pointer-events: none`、`aria-hidden`）。
- 验收口径：红框背景 `Vx` 只取该行分集 `currentVersionId` 对应当前版本的版本号；黄框背景数字与前景“当前未修改 n”中的 `n` 只统计当前版本 `unresolved` 意见数，前景与背景数值必须一致；背景字半透明、艺术斜体，被裁切后仍清晰可读其语义；已定稿行的绿色描边、选中态、批量删除、跳转“审阅”等既有行为不回归。
- `VISUAL-HARD-GATE-005`：以下项目内持久化图片是本轮的视觉硬门槛，不是普通灵感图：
  - 精确位置标注（红框/黄框）：[item-row-watermark-position-annotation-20260727.png](./visual-references/item-row-watermark-position-annotation-20260727.png)，SHA-256 `9e17b78954c2eb32865f8ba313c638e4f68ae7f00b08a4dd6abb4909e2d5a4a2`。图中红、黄线框只是标注辅助线，不属于界面，不得实现或出现在验收画面中。
  - 背景字视觉硬门槛 A：[item-row-watermark-style-hard-gate-a-20260727.png](./visual-references/item-row-watermark-style-hard-gate-a-20260727.png)，SHA-256 `7be9dc8e7a0b4348a35ee1397426929f2f4340a150f702b59b54c3e7d1dddcf1`。
  - 背景字视觉硬门槛 B：[item-row-watermark-style-hard-gate-b-20260727.png](./visual-references/item-row-watermark-style-hard-gate-b-20260727.png)，SHA-256 `f3e9e7624a291b087b6bc3800a25c6e9fb36294bf1046b68e594305884a09d99`。
- 视觉硬验收必须在真实渲染页面中，使用 Computer Use 对照上述图片检查背景字位置、尺寸、透明度、裁切范围与前景可读性。动态版本号与未修改数量允许随真实数据变化，但语义、来源、位置关系和视觉语言必须与基准一致。Playwright、DOM、CSS 检查、自动测试或单独截图只能辅助诊断，不能替代 `PASS_BROWSER`；未达到基准时本轮视觉验收为 `FAIL`，不得给功能 PASS。
- 实施与验收结果（2026-07-27）：已实施并通过验收。
  - 位置修正记录：首轮实现误将红框理解为“摘要单元格最右端”、黄框理解为“文字右侧独立区域”，经用户 2026-07-27 反馈打回后按标注图修正——红框背景 `Vx` 改为紧跟摘要文字之后的行中部位置（文档流内排版），黄框背景数字改为压在“当前未修改 n”前景文字右端之下（`z-index` 低于前景，向右略越出文字末端），两处均被各自框裁切。
  - 工程证据（修正后）：`vitest run src/modules/final-cut-review` 280/280 通过（含 `ProjectDetailItemList row watermarks` 两条用例，覆盖背景 `Vx` 取当前版本号、背景数字与前景“当前未修改 n”同值且仅统计当前版本 unresolved）；ESLint 无错误。
  - 视觉证据（修正后）：`PASS_BROWSER`。真实 Chrome 渲染截图对照 `VISUAL-HARD-GATE-005` 位置标注图：红框区背景 `V2`（归档项目态另验 `V3`）紧跟摘要文字之后；黄框区背景数字（`3`/`0`）压在“当前未修改 n”文字右端作背景、上下裁切；两处均为大号、半透明、艺术斜体，前景文字、状态胶囊、按钮层级与可读性不变；Computer Use 实击行内“查看”按钮成功跳转 `/edit/projects/prj_seed_final_cut/items/item_ep28`，背景字不阻断交互。证据存于 `/Volumes/App_Dev/审阅平台/.visual-review/`（整行截图、红框/黄框区域放大图、Computer Use 截屏 `cu-verify-fixed.png`）。

## [待实施] - 2026-07-25

### 新增

- `UI-HISTORY-VERSION-WATERMARK-001`：历史版本栏的版本卡片不再以前景普通标题显示版本号（如 `V1`）。每张卡片根据其自身对应的实际版本动态显示 `V1`、`V2`、`V3`……，将版本号作为位于卡片内部正中央的大号、半透明、艺术斜体背景文字。移除前景版本号后，前景实心信息整体上顶：状态文字占据原版本号标题的位置，文件名紧随其下；两者保持清晰可读，背景字不得遮挡点击或改变卡片交互。当前选中卡片的绿色描边及其余既有选中态行为保持不变。视觉参考：用户于 2026-07-25 提供的绿框历史版本卡片截图。
- 验收口径：前景不再重复出现普通版本号标题；历史版本栏中每张卡片的背景 `Vx` 只取该历史版本卡片自身的版本号，不取该剧集最新版本号，也不能固定写死为 `V1`；背景字在卡片内视觉居中、半透明并呈艺术斜体；状态文字准确顶到原版本号位置，文件名随前景信息一并上移且层级清楚；点击切换、焦点和选中绿框不回归。
- `UI-EPISODE-CARD-COMPACT-002`：工作台底部“剧集列表”的剧集卡片采用用户于 2026-07-25 最新提供的单卡布局，替换并取消此前登记的 `UI-EPISODE-LATEST-VERSION-WATERMARK-002`。剧集卡片不显示任何版本号背景字，也不显示前景 `Vx`、版本数量或“X个版本 · 当前 Vx”摘要行；改为显示该集当前版本“未修改意见数量”的大号、半透明、艺术斜体背景数字，例如当前版本有 8 条未修改意见时显示背景数字 `8`。卡片按最新参考图排版：左侧由上到下排列集数、包含具体数字的完整前景文案“当前版本未修改 8”、该集当前最新版本的原文件名；大号数量背景位于卡片中央并处在前景内容之后；当前审阅状态胶囊位于右侧并纵向居中。背景数字和前景具体数字显示同一个动态计数值，用完整前景文案明确背景数字的业务含义。当前选中剧集的绿色描边、底部绿色高亮及其余既有选中态行为保持不变。参考图中的蓝色线框只是标注辅助线，不属于界面，不得实现或出现在验收画面中；其余布局以本次最新截图为准。
- 验收口径：所有剧集卡片均无版本号背景和其他 `Vx` 展示；每张卡片的背景数字和前景“当前版本未修改 N”中的 `N`，只统计该集 `currentVersionId` 对应当前版本的未修改意见数量，不得混入历史版本、已修改意见、已删除意见、其他剧集或当前正在查看的历史版本，且前景与背景数值必须始终一致。每张卡片准确显示自身集数、包含具体数量的完整前景文案、当前最新版本原文件名和当前审阅状态；左侧三行信息、中央数量背景、右侧状态胶囊的位置关系与最新参考图一致，背景数字不得压低前景可读性；卡片内不得出现蓝色标注线框。背景数字保持半透明、艺术斜体；点击切集、键盘焦点、横向滚动、选中绿框和底部高亮不回归。
- `UI-BACKGROUND-METRIC-SCOPE-ISOLATION`：需求 1 的“历史版本”卡片背景仍显示该历史版本卡片自身的 `Vx`；需求 2 的“剧集列表”卡片背景只显示该剧集当前版本的未修改意见数量。两者的数据类型和来源必须隔离，选择或播放历史版本不得改变剧集卡片的数量背景，切换剧集也不得把版本号或其他剧集的数量写入剧集卡片背景。
- `VISUAL-HARD-GATE-004`：以下两张项目内持久化图片是本轮两个卡片的视觉硬门槛，不是普通灵感图或可选建议：
  - 剧集列表卡片：[episode-card-unresolved-count-hard-gate-20260725.png](./visual-references/episode-card-unresolved-count-hard-gate-20260725.png)，SHA-256 `458111052176c83197e84ecc66b705f90246b5441beb791a96cbbe5daba375f4`。图中的蓝色线框必须忽略，不属于目标界面。
  - 历史版本卡片：[history-version-card-hard-gate-20260725.png](./visual-references/history-version-card-hard-gate-20260725.png)，SHA-256 `6634436b8359e87fcc3d507d8f4f1fd6f4c81c12744761d8f5412781a8afb067`。
- 视觉硬验收必须在真实渲染页面中，使用 Codex 内置 Computer Use 完成相关卡片的可见点击、切集和历史版本切换后，再对照上述图片检查信息层级、左中右布局、背景字/数字的尺寸与透明度、状态胶囊、选中描边、底部高亮、间距和前景可读性。动态集数、文件名、状态、版本号与未修改数量允许随真实数据变化，但语义、来源、层级、位置关系和视觉语言必须与基准一致。Playwright、DOM、CSS 检查、自动测试或单独截图只能辅助诊断，不能替代 `PASS_BROWSER`；任何一张卡片未达到对应基准时，本轮视觉验收为 `FAIL`，不得给功能 PASS。
- `PROJECT-PACKAGE-LIMITS-003`：项目定稿原片打包上限调整为最多 `200` 个文件，所有待打包定稿原片的总大小上限调整为 `50 GiB`（`53,687,091,200` 字节），替换现有的 `100` 个文件和 `5 GiB` 限制。文件数量超过 200 时继续使用“项目定稿原片数量超出打包限制”错误语义；总大小超过 50 GiB 时继续使用“项目定稿原片总大小超出打包限制”错误语义。总大小按进入项目包的定稿原片原始字节数求和，不以 ZIP 压缩后大小计算。
- 验收口径：第 200 个文件允许进入打包，第 201 个文件被拒绝；定稿原片总大小等于 50 GiB 时允许，超过 50 GiB 时被拒绝。下游施工必须同步对齐应用设置、配置校验上界、Compose 默认值、示例配置、相关测试以及会导致单个 50 GiB 项目包被更低默认值提前拦截的关联包存储配额；在磁盘空间和其他既有安全条件满足时，50 GiB 不能只停留在名义配置。保留现有幂等、队列、文件完整性、磁盘安全和失败恢复行为。
- 当前状态：仅完成需求登记，尚未实施、测试或进行真实 Browser 视觉验收。

## [v1.4] - 2026-07-16

### 修改

- 明确顶部入口导航的非对称可见性：剪辑模式只显示“剪辑入口”；审阅模式同时显示“剪辑入口”和“成片审阅”，并允许返回剪辑入口。
- 明确隐藏跨入口链接不等于路由或权限封禁，`/review` 独立路由与服务端能力校验保持不变。
